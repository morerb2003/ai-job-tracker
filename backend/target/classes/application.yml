<<<<<<< HEAD
server:
  port: 8080

spring:

  profiles:
    active: dev

  application:
    name: jobtracker

  datasource:
    url: jdbc:postgresql://localhost:5432/job_tracker
    username: postgres
    password: More@2525

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

management:
=======
server:
  port: 8080

spring:

  profiles:
    active: dev

  application:
    name: jobtracker

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/job_tracker}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
management:
>>>>>>> feature/authentication
  endpoints:
    web:
      exposure:
        include: health,info

<<<<<<< HEAD
=======
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION_MS:900000} # 15 minutes in milliseconds
  refresh-expiration: ${JWT_REFRESH_EXPIRATION_MS:604800000} # 7 days in milliseconds

>>>>>>> feature/authentication
logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG
